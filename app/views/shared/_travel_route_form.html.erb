<%= form_for(@travel_route) do |f| %>
    <%= render 'shared/error_messages', object: f.object %>
    <div class="field">
        <%= f.text_area :distance, placeholder: "Enter the distance..." %>
    </div>
    <%= f.submit "Post", class: "btn btn-primary" %>
<% end %>
